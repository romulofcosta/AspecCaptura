@using pwa_camera_poc_blazor.Models

<div id="item-modal" class="modal @(IsOpen ? "" : "hidden")" style="@(IsOpen ? "display:flex;" : "display:none;")">
    <div class="modal-content">
        @if (Item != null)
        {
            <div class="modal-header">
                <h2 id="modal-title">Detalhes do Item</h2>
                <button id="close-modal-btn" class="icon-btn" @onclick="Close">✕</button>
            </div>
            <div class="modal-body">
                <div class="modal-image-container">
                    <img id="modal-image" src="@Item.CoverImage" alt="@Item.Nome">
                </div>
                <div class="modal-details">
                    <div class="detail-row">
                        <span class="detail-label">Nome:</span>
                        <span id="detail-name" class="detail-value">@Item.Nome</span>
                    </div>
                    <div class="detail-row">
                        <span class="detail-label">Código:</span>
                        <span id="detail-code" class="detail-value">@Item.Codigo</span>
                    </div>
                    <div class="detail-row">
                        <span class="detail-label">Categoria:</span>
                        <span id="detail-category" class="detail-value">@Item.Category</span>
                    </div>
                    <div class="detail-row">
                        <span class="detail-label">Localização:</span>
                        <span id="detail-location" class="detail-value">@Item.Localizacao</span>
                    </div>
                    <div class="detail-row">
                        <span class="detail-label">Observações:</span>
                        <span id="detail-observations" class="detail-value">@Item.Observacoes</span>
                    </div>
                    <div class="detail-row">
                        <span class="detail-label">Data de Cadastro:</span>
                        <span id="detail-date" class="detail-value">@Item.DataHora.ToString("dd/MM/yyyy HH:mm")</span>
                    </div>
                    <div class="detail-row">
                        <span class="detail-label">Status:</span>
                        <span id="detail-sync-status" class="detail-value">@(Item.Sincronizado ? "Sincronizado" :
                        "Pendente")</span>
                    </div>
                </div>
            </div>
            <div class="modal-actions">
                <div class="edit-actions">
                    <button id="edit-item-btn" class="btn btn-secondary edit-btn" @onclick="HandleEdit">✏️ Editar</button>
                    <button id="close-item-btn" class="btn btn-secondary close-btn" @onclick="Close">Fechar</button>
                </div>
                <button id="delete-item-btn" class="btn btn-danger" @onclick="HandleDelete">🗑️ Excluir</button>
            </div>
        }
    </div>
</div>

@code {
    [Parameter] public bool IsOpen { get; set; }
    [Parameter] public ItemPatrimonio? Item { get; set; }
    [Parameter] public EventCallback OnClose { get; set; }
    [Parameter] public EventCallback<ItemPatrimonio> OnDelete { get; set; }
    [Parameter] public EventCallback<ItemPatrimonio> OnEdit { get; set; }

    private async Task Close() => await OnClose.InvokeAsync();
    private async Task HandleDelete() => await OnDelete.InvokeAsync(Item!);
    private async Task HandleEdit() => await OnEdit.InvokeAsync(Item!);
}
